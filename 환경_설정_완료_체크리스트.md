# 환경 설정 완료 체크리스트 ✅

## ✅ 완료된 작업

### 1. 배치 파일 경로 수정
- ✅ `start-backend.bat` - 동적 경로 설정 완료
- ✅ `start-frontend.bat` - 동적 경로 설정 완료  
- ✅ `start-all.bat` - 동적 경로 설정 완료

### 2. 백엔드 환경
- ✅ Java 17 설치 확인: `openjdk version "17.0.2"`
- ✅ Maven 설치 확인: `apache-maven-3.9.6`
- ✅ 백엔드 의존성 설치 완료

### 3. 프론트엔드 환경
- ✅ Node.js 설치 확인: `v18.19.0`
- ✅ npm 설치 확인: `10.2.3`
- ✅ `react-scripts` 버전 수정 완료 (`5.0.1`)
- ✅ 프론트엔드 의존성 설치 완료 (1499 packages)

## 🚀 실행 방법

### 전체 시스템 실행
```cmd
start-all.bat
```

### 개별 실행

**백엔드만 실행:**
```cmd
start-backend.bat
```
- 백엔드는 `http://localhost:8080`에서 실행됩니다
- 첫 실행 시 Maven 의존성 다운로드로 시간이 걸릴 수 있습니다

**프론트엔드만 실행:**
```cmd
start-frontend.bat
```
- 프론트엔드는 `http://localhost:3000`에서 실행됩니다
- 브라우저가 자동으로 열립니다

## ✅ 정상 작동 확인 방법

### 1. 백엔드 확인
1. `start-backend.bat` 실행
2. 콘솔에 다음과 같은 메시지가 나타나면 정상:
   ```
   Started DwfwBackendApplication in X.XXX seconds
   ```
3. 브라우저에서 `http://localhost:8080` 접속
4. 또는 PowerShell에서 확인:
   ```powershell
   curl http://localhost:8080/api/auth/init-admin
   ```

### 2. 프론트엔드 확인
1. `start-frontend.bat` 실행
2. 콘솔에 다음과 같은 메시지가 나타나면 정상:
   ```
   Compiled successfully!
   You can now view dwfw-frontend in the browser.
   Local:            http://localhost:3000
   ```
3. 브라우저가 자동으로 열리고 로그인 페이지가 표시됩니다

### 3. 전체 시스템 확인
1. `start-all.bat` 실행
2. 백엔드 서버가 먼저 시작됩니다 (약 15초 대기)
3. 프론트엔드 서버가 자동으로 시작됩니다
4. 브라우저에서 `http://localhost:3000` 접속
5. 로그인 페이지에서 다음 정보로 로그인:
   - **사용자명**: ADMIN
   - **비밀번호**: 1

## 🔍 문제 해결

### 백엔드가 시작되지 않는 경우
- 포트 8080이 사용 중인지 확인:
  ```powershell
  netstat -ano | findstr ":8080"
  ```
- Java 경로가 올바른지 확인
- `backend/data/` 디렉토리가 존재하는지 확인

### 프론트엔드가 시작되지 않는 경우
- 포트 3000이 사용 중인지 확인:
  ```powershell
  netstat -ano | findstr ":3000"
  ```
- `node_modules`가 제대로 설치되었는지 확인:
  ```powershell
  cd frontend
  npm install
  ```

### CORS 오류가 발생하는 경우
- 백엔드가 실행 중인지 확인
- `frontend/public/config.js`의 백엔드 URL 확인

## 📝 참고사항

- 백엔드는 첫 실행 시 Maven이 의존성을 다운로드하므로 시간이 걸릴 수 있습니다
- 프론트엔드는 첫 실행 시 컴파일 시간이 걸릴 수 있습니다
- 두 서버 모두 실행 중인 상태에서 개발을 진행하세요
- 서버를 종료하려면 각 터미널 창에서 `Ctrl+C`를 누르세요
